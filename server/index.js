// we can use the import syntax by using the following script: "type": "module" in pack.json
import express from 'express';
import bodyParser from 'body-parser';
import { Mongoose } from 'mongoose';
import cors from 'cors';
